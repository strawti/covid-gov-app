import 'package:flutter/material.dart';

// Componentes 

// Método para a estrutura do app bar (menu superior)
Widget nossoAppBar() {
  return AppBar(
    backgroundColor: Colors.white,
    elevation: 0,
    iconTheme: const IconThemeData(color: Colors.blue),
  );
}
